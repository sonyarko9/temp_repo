// CommonJS, every file is a module
// Modules - Encapsulated code

const female ='Esther'
const male = 'Samuel'

const sayHi = (name) =>{
  console.log(`Hello there ${name}`)
}
 sayHi('samuel');
 sayHi(female)
 sayHi (male)